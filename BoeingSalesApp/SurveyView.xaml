<Page
    x:Name="Survey"
    x:Class="BoeingSalesApp.SurveyView"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BoeingSalesApp"
    xmlns:common="using:BoeingSalesApp.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <x:String x:Key="AppName">Exit Survey</x:String>

        <!-- This style is generated by Blend. -->
        <Style x:Key="RadioButtonStyle1" TargetType="RadioButton">
        	<Setter Property="Background" Value="Transparent"/>
        	<Setter Property="Foreground" Value="{ThemeResource RadioButtonContentForegroundThemeBrush}"/>
        	<Setter Property="Padding" Value="1,4,0,0"/>
        	<Setter Property="HorizontalAlignment" Value="Stretch"/>
        	<Setter Property="VerticalAlignment" Value="Center"/>
        	<Setter Property="HorizontalContentAlignment" Value="Left"/>
        	<Setter Property="VerticalContentAlignment" Value="Top"/>
        	<Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}"/>
        	<Setter Property="FontSize" Value="{ThemeResource ControlContentThemeFontSize}"/>
        	<Setter Property="Template">
        		<Setter.Value>
        			<ControlTemplate TargetType="RadioButton">
        				<Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
        					<VisualStateManager.VisualStateGroups>
        						<VisualStateGroup x:Name="CommonStates">
        							<VisualState x:Name="Normal"/>
        							<VisualState x:Name="PointerOver"/>
        							<VisualState x:Name="Pressed"/>
        							<VisualState x:Name="Disabled">
        								<Storyboard>
        									<ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Foreground" Storyboard.TargetName="ContentPresenter">
        										<DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource RadioButtonContentDisabledForegroundThemeBrush}"/>
        									</ObjectAnimationUsingKeyFrames>
        								</Storyboard>
        							</VisualState>
        						</VisualStateGroup>
        						<VisualStateGroup x:Name="CheckStates">
        							<VisualState x:Name="Checked">
        								<Storyboard>
        									<DoubleAnimation Duration="0" To="1" Storyboard.TargetProperty="Opacity" Storyboard.TargetName="CheckGlyph"/>
        								</Storyboard>
        							</VisualState>
        							<VisualState x:Name="Unchecked"/>
        							<VisualState x:Name="Indeterminate"/>
        						</VisualStateGroup>
        						<VisualStateGroup x:Name="FocusStates">
        							<VisualState x:Name="Focused">
        								<Storyboard>
        									<DoubleAnimation Duration="0" To="1" Storyboard.TargetProperty="Opacity" Storyboard.TargetName="FocusVisualWhite"/>
        									<DoubleAnimation Duration="0" To="1" Storyboard.TargetProperty="Opacity" Storyboard.TargetName="FocusVisualBlack"/>
        								</Storyboard>
        							</VisualState>
        							<VisualState x:Name="Unfocused"/>
        							<VisualState x:Name="PointerFocused"/>
        						</VisualStateGroup>
        					</VisualStateManager.VisualStateGroups>
        					<Grid Margin="0,0,10,0">
        						<Grid.ColumnDefinitions>
        							<ColumnDefinition Width="29"/>
        							<ColumnDefinition Width="*"/>
        						</Grid.ColumnDefinitions>
        						<Grid VerticalAlignment="Top" Margin="0">
        							<Ellipse x:Name="BackgroundEllipse" Fill="#CCA3A0A4" Height="23" Stroke="#CC5F5E5E" UseLayoutRounding="False" Width="23" StrokeThickness="5" Margin="0"/>
        							<Ellipse x:Name="CheckGlyph" Fill="{StaticResource BoeingBlue}" Height="10" Opacity="0" UseLayoutRounding="False" Width="10"/>
        							<Rectangle x:Name="FocusVisualWhite" Height="29" Opacity="0" StrokeDashOffset="1.5" StrokeEndLineCap="Square" Stroke="{ThemeResource FocusVisualWhiteStrokeThemeBrush}" StrokeDashArray="1,1" Width="29"/>
        							<Rectangle x:Name="FocusVisualBlack" Height="29" Opacity="0" StrokeDashOffset="0.5" StrokeEndLineCap="Square" Stroke="{ThemeResource FocusVisualBlackStrokeThemeBrush}" StrokeDashArray="1,1" Width="29"/>
        						</Grid>
        						<ContentPresenter x:Name="ContentPresenter" AutomationProperties.AccessibilityView="Raw" ContentTemplate="{TemplateBinding ContentTemplate}" ContentTransitions="{TemplateBinding ContentTransitions}" Content="{TemplateBinding Content}" Grid.Column="1" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
        					</Grid>
        				</Border>
        			</ControlTemplate>
        		</Setter.Value>
        	</Setter>
        </Style>
    </Page.Resources>

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid Background="White">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button and page title -->
        <Grid Grid.Row="0" Background="{StaticResource BoeingBlue}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton" Margin="39,59,39,0"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"
                        VerticalAlignment="Top"
                        Tapped="backButton_Tapped"/>
            <TextBlock x:Name="pageTitle" Text="{StaticResource AppName}" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40" FontFamily="Global User Interface"/>
        </Grid>

        <Grid Grid.Row="1" Margin="39,15,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,30">
                <TextBlock FontSize="24" Text="Primary Contact" 
                          Foreground="{StaticResource BoeingBlue}" FontWeight="Bold"/>
                <TextBox x:Name="ContactField" Width="250" Margin="30,0,0,0" 
                         BorderBrush="{StaticResource BoeingBlue}"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,0,0,30">
                <TextBlock FontSize="24" Text="How would you rate this meeting ?" Foreground="{StaticResource BoeingBlue}" FontWeight="Bold"/>

                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,30">
                    <RadioButton x:Name="ratingA" FontSize="24" Foreground="Black" Content="A" Style="{StaticResource RadioButtonStyle1}"/>
                    <RadioButton x:Name="ratingB" FontSize="24" Foreground="Black" Content="B" Style="{StaticResource RadioButtonStyle1}"/>
                    <RadioButton x:Name="ratingC" FontSize="24" Foreground="Black" Content="C" Style="{StaticResource RadioButtonStyle1}"/>
                    <RadioButton x:Name="ratingD" FontSize="24" Foreground="Black" Content="D" Style="{StaticResource RadioButtonStyle1}"/>
                    <RadioButton x:Name="ratingF" FontSize="24" Foreground="Black" Content="F" Style="{StaticResource RadioButtonStyle1}"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,0,0,30">
                <TextBlock FontSize="24" Text="Action Items ..." Foreground="{StaticResource BoeingBlue}" FontWeight="Bold"/>
                <TextBlock FontSize="24" Text="Key Takeaways ..." Foreground="{StaticResource BoeingBlue}" FontWeight="Bold" Margin="0,20,0,0"/>
            </StackPanel>
            
            <StackPanel Grid.Row="3" Orientation="Vertical" Margin="0,0,0,30">
                <TextBlock FontSize="24" Text="Comment" Foreground="{StaticResource BoeingBlue}" FontWeight="Bold"/>
                <RichEditBox x:Name="CommentBox" Width="1000" Height="200" 
                         HorizontalAlignment="Left" Margin="30,0,0,0" BorderBrush="{StaticResource BoeingBlue}"
                         TextWrapping="Wrap" AcceptsReturn="True" />
            </StackPanel>

            <Button x:Name="Submit" Grid.Row="4" Background="{StaticResource BoeingBlue}" Foreground="White" 
                    Content="Submit" Tapped="Submit_Tapped" />
        </Grid>
    </Grid>
</Page>
