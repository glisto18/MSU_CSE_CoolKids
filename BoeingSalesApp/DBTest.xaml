<Page
    x:Class="BoeingSalesApp.DBTest"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BoeingSalesApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Name="uxCategoryPanel" Margin="10">
            <TextBlock>Test Name</TextBlock>
            <TextBox Name="uxTextBox" Text="{Binding mTest_Name, Mode=TwoWay}"></TextBox>
            <Button Name="Save" Click="Save_Click">Save</Button>
            <Button Name="Delete" Click="Delete_Click">Delete</Button>
            <ListView Name="TestClasses" ItemsSource="{Binding}" SelectionChanged="TestClasses_OnSelectionChanged">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="4">
                            <TextBlock Text="{Binding mTest_Name}"></TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <TextBlock Name="Status" FontSize="16" Foreground="GreenYellow"></TextBlock>
        </StackPanel>
        </Grid>
</Page>
